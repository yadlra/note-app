// function Note(text){
//   var note = new Note();
//   assert.isTrue(note.text(string) === string);
// };
//
// Note();










// var assert = {
//   isTrue: function(!note.text(string) {
//     if (!note.text(string) === string) {
//     throw new Error("Assertion failed: " + note.text(string) === string + " is not thruthy");
//    }
//  };
// };
